# Harbinger Learned Rules

## Lessons Learned

- **When `read_board` returns "Nothing posted for topic 'tradesmen'" — call `divine()` IMMEDIATELY.** Do NOT retry `read_board`. You are already in The Agency from step 1 — just call `divine()` where you are. Do not add another teleport.

## Rules of Engagement

## Verb Mapping
