#!moo verb sailor_fcn --on "sailor" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL: SAILOR-FCN / combined OBJECT-FUNCTION dispatch
# Invoked by ``_.thing.dispatch_object_function(obj, verb, prep, iobj)``
# which looks up ``obj.action`` and calls ``obj.invoke_verb(action_atom, ...)``.

player = context.player
the_verb = args[0] if len(args) > 0 else None
prep = args[1] if len(args) > 1 else None
iobj = args[2] if len(args) > 2 else None
the_player_verb = the_verb

if the_verb == "tell":
    player.zstate_set("P-CONT", False)
    player.zstate_set("QUOTE-FLAG", False)
    print("You can't talk to the sailor that way.")
    return
elif the_verb == "examine":
    print("There is no sailor to be seen.")
    return
elif the_verb == "hello":
    player.zstate_set("HS", (player.zstate_get("HS") + 1))
    if player.zstate_get("HS") % 20 == 0:
        print("You seem to be repeating yourself.")
        return
    elif player.zstate_get("HS") % 10 == 0:
        print("I think that phrase is getting a bit worn out.")
        return
    else:
        print("Nothing happens here.")
        return
return False
