#!moo verb walk go run proceed step north n south s east e west w northeast ne northwest nw southeast se southwest sw up u down d in out land --on "Actor" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

"""Player command for WALK: traverse an exit Object.

Bare directions (``n``, ``north``, ``up`` etc.) are
registered here too so a player can type ``n`` instead of
``go n``; in that case verb_name carries the direction
and parser has no dobj."""

from moo.sdk import context

parser = context.parser
DIR_WORDS = {
    "north",
    "n",
    "south",
    "s",
    "east",
    "e",
    "west",
    "w",
    "northeast",
    "ne",
    "northwest",
    "nw",
    "southeast",
    "se",
    "southwest",
    "sw",
    "up",
    "u",
    "down",
    "d",
    "in",
    "out",
    "land",
}
if parser.has_dobj_str():
    _.walk(parser.get_dobj_str())
elif len(parser.words) > 1:
    _.walk(parser.words[1])
elif verb_name in DIR_WORDS:
    _.walk(verb_name)
else:
    print("Where do you want to go?")
