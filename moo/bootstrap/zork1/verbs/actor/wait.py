#!moo verb wait z --on "Actor" --dspec none

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, task_time_low

# ZIL routine: V-WAIT
# params: NUM

player = context.player
num = args[0] if len(args) > 0 else 3

print("Time passes...")
while True:
    if task_time_low():
        print("[zil] long-running loop in V-WAIT; aborting (bug — please report).")
        return False
    if (num := (num - 1)) < 0:
        break
    elif _.thing.clocker():
        break
return player.zstate_set("CLOCK-WAIT", True)
