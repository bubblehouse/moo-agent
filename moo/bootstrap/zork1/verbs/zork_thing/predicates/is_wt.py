#!moo verb is_wt --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: WT?
# params: PTR, BIT, B1
# aux: OFFS, TYP

player = context.player
ptr = args[0] if len(args) > 0 else None
bit = args[1] if len(args) > 1 else None
b1 = args[2] if len(args) > 2 else 5
offs = player.zstate_get("P-P1OFF")
typ = 0

if (((typ := _.table_get(ptr, player.zstate_get("P-PSOFF"))) or 0) & ((bit) or 0)) != 0:
    if b1 > 4:
        return True
    else:
        typ = ((typ) or 0) & ((player.zstate_get("P-P1BITS")) or 0)
        if typ != b1:
            offs = offs + 1
        # ZIL: <GETB ...>
        return _.table_get(ptr, offs)
