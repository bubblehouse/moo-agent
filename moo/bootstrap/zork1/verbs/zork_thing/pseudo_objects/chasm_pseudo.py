#!moo verb chasm_pseudo --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: CHASM-PSEUDO

parser = context.parser
the_player_verb = context.parser.words[0].lower() if context.parser is not None and context.parser.words else verb_name

if the_player_verb == "leap" or (
    the_player_verb in ["put", "place", "insert"]
    and (parser.get_dobj() if parser.has_dobj_str() else None) == lookup("me")
):
    print("You look before leaping, and realize that you would never survive.")
    return
elif the_player_verb == "cross":
    print("It's too far to jump, and there's no bridge.")
    return
elif the_player_verb in ["put", "place", "insert", "throw-off"] and (
    parser.get_iobj() if parser.has_iobj() else None
) == lookup("pseudo_object"):
    print(
        "The " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " drops out of sight into the chasm."
    )
    return _.remove((parser.get_dobj() if parser.has_dobj_str() else None))
