#!moo verb pre_turn --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: PRE-TURN

player = context.player
parser = context.parser

if (
    (
        (parser.get_iobj() if parser.has_iobj() else None) in (False, player.zstate_get("ROOMS"))
        and not (parser.get_dobj() if parser.has_dobj_str() else None) == lookup("book")
    )
    if player.zstate_get("ZORK-NUMBER") == 1
    else (parser.get_iobj() if parser.has_iobj() else None) in (False, player.zstate_get("ROOMS"))
):
    print("Your bare hands don't appear to be enough.")
    return
elif not (parser.get_dobj() if parser.has_dobj_str() else None).flag("turnable"):
    print("You can't turn that!")
    return
