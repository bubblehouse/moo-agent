#!moo verb pre_turn --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL routine: PRE-TURN

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("I don't know how to do that.")
    return True

if (
    (prsi in (False, player.zstate_get("ROOMS")) and not prso == lookup("book"))
    if player.zstate_get("ZORK-NUMBER") == 1
    else prsi in (False, player.zstate_get("ROOMS"))
):
    print("Your bare hands don't appear to be enough.")
    return True
elif not (prso.flag("turnable") if prso else None):
    print("You can't turn that!")
    return True
