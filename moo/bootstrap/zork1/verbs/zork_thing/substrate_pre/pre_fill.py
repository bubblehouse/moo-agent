#!moo verb pre_fill --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: PRE-FILL
# aux: TX

player = context.player
parser = context.parser
global_water = lookup("global_water")
water = lookup("water")
tx = 0

if not (parser.get_iobj() if parser.has_iobj() else None):
    tx = getpt(player.here(), player.zstate_get("P?GLOBAL"))
    if tx and _.zork_thing.zmemqb(global_water, tx, (ptsize(tx) - 1)):
        _.perform("fill", (parser.get_dobj() if parser.has_dobj_str() else None), global_water)
        return True
    elif water.location == player.location:
        _.perform("fill", (parser.get_dobj() if parser.has_dobj_str() else None), water)
        return True
    else:
        print("There is nothing to fill it with.")
        return True
if (parser.get_iobj() if parser.has_iobj() else None) == water:
    return False
elif not (parser.get_iobj() if parser.has_iobj() else None) == global_water:
    _.perform(
        "put",
        (parser.get_iobj() if parser.has_iobj() else None),
        (parser.get_dobj() if parser.has_dobj_str() else None),
    )
    return True
