#!moo verb pre_read --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: PRE-READ

player = context.player
parser = context.parser

if not player.zstate_get("LIT"):
    print("It is impossible to read in the dark.")
    return
elif (parser.get_iobj() if parser.has_iobj() else None) and not (
    parser.get_iobj().flag("transparent") if parser.has_iobj() else None
):
    print("How does one look through a " + (parser.get_iobj().desc() if parser.has_iobj() else "") + "?")
    return
