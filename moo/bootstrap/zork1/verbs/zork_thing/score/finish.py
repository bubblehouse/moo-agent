#!moo verb finish --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, task_time_low

# ZIL routine: FINISH
# aux: WRD

player = context.player
parser = context.parser
wrd = 0

# ZIL: <V-SCORE ...>
player.score()
while True:
    if task_time_low():
        print("[zil] long-running loop in FINISH; aborting (bug — please report).")
        return False
    print()
    print(
        "Would you like to restart the game from the beginning, restore a saved\ngame position, or end this session of the game?\n\n(Type RESTART, RESTORE, or QUIT):\n\n>",
        end="",
    )
    # ZIL: <READ ...>
    return False
    wrd = parser.words[(1) - 1] if context.parser is not None and len(parser.words) >= (1) else ""
    if wrd == player.zstate_get("W?RESTART"):
        print("Restart not supported in DjangoMOO.")
        print("Failed.")
        return
    elif wrd == player.zstate_get("W?RESTORE"):
        if False:  # pylint: disable=using-constant-test
            print("Ok.")
            return
        else:
            print("Failed.")
            return
    elif wrd in (player.zstate_get("W?QUIT"), player.zstate_get("W?Q")):
        print("Goodbye.")
        return
