#!moo verb score_upd --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: SCORE-UPD
# params: NUM

player = context.player
num = args[0] if len(args) > 0 else None

player.zstate_set("BASE-SCORE", (player.zstate_get("BASE-SCORE") + num))
player.zstate_set("SCORE", (player.zstate_get("SCORE") + num))
if player.zstate_get("SCORE") == 350 and not player.zstate_get("WON-FLAG"):
    player.zstate_set("WON-FLAG", True)
    lookup("map").set_flag("invisible", False)
    lookup("west_of_house").set_flag("touchbit", False)
    print('An almost inaudible voice whispers in your ear, "Look to your treasures\nfor the final secret."')
return True
