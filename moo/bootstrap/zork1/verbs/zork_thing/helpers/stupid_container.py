#!moo verb stupid_container --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name

# ZIL routine: STUPID-CONTAINER
# params: OBJ, STR

parser = context.parser
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
obj = args[0] if len(args) > 0 else None
str_v = args[1] if len(args) > 1 else None
the_player_verb = invoked_verb_name(verb_name)

if the_player_verb in ["open", "close", "shut"]:
    print("The " + str_v + " are safely inside; there's no need to do that.")
    return
elif the_player_verb in ["look-inside", "examine", "x", "describe", "what"]:
    print("There are lots of " + str_v + " in there.")
    return
elif the_player_verb in ["put", "place", "insert"] and prsi == obj:
    print("Don't be silly. It wouldn't be a " + obj.desc() + " anymore.")
    return
