#!moo verb zmemq --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import task_time_low

# ZIL routine: ZMEMQ
# params: ITM, TBL, SIZE
# aux: CNT

itm = args[0] if len(args) > 0 else None
tbl = args[1] if len(args) > 1 else None
size = args[2] if len(args) > 2 else -1
cnt = 1

if not tbl:
    return False
if not size < 0:
    cnt = 0
else:
    size = _.table_get(tbl, 0)
while True:
    if task_time_low():
        print("[zil] long-running loop in ZMEMQ; aborting (bug — please report).")
        return False
    if itm == _.table_get(tbl, cnt):
        return _.rest(tbl, (cnt * 2))
    elif igrtr_p(cnt, size):
        return False
