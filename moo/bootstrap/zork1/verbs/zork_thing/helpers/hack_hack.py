#!moo verb hack_hack --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL routine: HACK-HACK
# params: STR

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
str_v = args[0] if len(args) > 0 else None
the_player_verb = context.parser.words[0].lower() if context.parser is not None and context.parser.words else verb_name

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("I don't know how to do that.")
    return

if prso.location == lookup("global_objects") and the_player_verb in ["wave", "raise", "lower"]:
    print("The " + (prso.desc() if prso else "") + " isn't here!")
    return
else:
    print(str_v + (prso.desc() if prso else "") + _.pick(player.zstate_get("HO-HUM")))
    return
