#!moo verb deposit_booty --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup, task_time_low

# ZIL routine: DEPOSIT-BOOTY
# params: RM
# aux: X, N, FLG

player = context.player
egg = lookup("egg")
rm = args[0] if len(args) > 0 else None
x = 0
n = 0
flg = 0

x = lookup("thief").contents.first()
while True:
    if task_time_low():
        print("[zil] long-running loop in DEPOSIT-BOOTY; aborting (bug — please report).")
        return False
    if not x:
        return flg
    n = _.next_sibling(x)
    if x in (lookup("stiletto"), lookup("large_bag")):
        pass
    elif x.getp("tvalue") > 0:
        x.moveto(rm)
        flg = True
        if x == egg:
            player.zstate_set("EGG-SOLVE", True)
            egg.set_flag("open", True)
    x = n
