#!moo verb idrop --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: IDROP

player = context.player
parser = context.parser

if (
    not (parser.get_dobj() if parser.has_dobj_str() else None).location == player
    and not (parser.get_dobj() if parser.has_dobj_str() else None).location.location == player
):
    print("You're not carrying the " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + ".")
    return False
elif not (parser.get_dobj() if parser.has_dobj_str() else None).location == player and not (
    parser.get_dobj() if parser.has_dobj_str() else None
).location.flag("open"):
    print("The " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " is closed.")
    return False
else:
    (parser.get_dobj() if parser.has_dobj_str() else None).moveto(player.location)
    return True
