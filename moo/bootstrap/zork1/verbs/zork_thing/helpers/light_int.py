#!moo verb light_int --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: LIGHT-INT
# params: OBJ, TBL, TICK

player = context.player
obj = args[0] if len(args) > 0 else None
tbl = args[1] if len(args) > 1 else None
tick = args[2] if len(args) > 2 else None

if tick == 0:
    obj.set_flag("onbit", False)
    obj.set_flag("rmungbit", True)
if _.zork_thing.is_held(obj) or obj.location == player.here():
    if tick == 0:
        print("You'd better have more light than from the " + obj.desc() + ".")
        return
    else:
        print(_.table_get(tbl, 1))
        return
