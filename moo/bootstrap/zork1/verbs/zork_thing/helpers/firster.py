#!moo verb firster --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: FIRSTER
# params: OBJ, LEVEL

player = context.player
obj = args[0] if len(args) > 0 else None
level = args[1] if len(args) > 1 else None

if obj == lookup("trophy_case"):
    print("Your collection of treasures consists of:")
    return
elif obj == player:
    print("You are carrying:")
    return
elif not obj.location == player.zstate_get("ROOMS"):
    if level > 0:
        print(_.table_get(player.zstate_get("INDENTS"), level), end="")
    if obj.flag("surfacebit"):
        print("Sitting on the " + obj.desc() + " is: ")
        return
    elif obj.flag("actorbit"):
        print("The " + obj.desc() + " is holding: ")
        return
    else:
        print("The " + obj.desc() + " contains:")
        return
