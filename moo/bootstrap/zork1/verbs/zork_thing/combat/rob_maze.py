#!moo verb rob_maze --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

import random
from moo.sdk import lookup, task_time_low

# ZIL routine: ROB-MAZE
# params: RM
# aux: X, N

rm = args[0] if len(args) > 0 else None
x = 0
n = 0

x = rm.contents.first()
while True:
    if task_time_low():
        print("[zil] long-running loop in ROB-MAZE; aborting (bug — please report).")
        return False
    if not x:
        return False
    n = _.next_sibling(x)
    if x.flag("takeable") and not x.flag("invisible") and random.randint(1, 100) <= 40:
        print(
            'You hear, off in the distance, someone saying "My, I wonder what\nthis fine '
            + x.desc()
            + ' is doing here."'
        )
        if random.randint(1, 100) <= 60:
            x.moveto(lookup("thief"))
            x.set_flag("touchbit", True)
            x.set_flag("invisible", True)
        break
    x = n
