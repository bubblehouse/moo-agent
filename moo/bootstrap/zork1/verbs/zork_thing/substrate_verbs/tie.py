#!moo verb tie fasten secure attach --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-TIE

player = context.player
parser = context.parser

if (parser.get_iobj() if parser.has_iobj() else None) == player:
    print("You can't tie anything to yourself.")
    return
else:
    print("You can't tie the " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " to that.")
    return
