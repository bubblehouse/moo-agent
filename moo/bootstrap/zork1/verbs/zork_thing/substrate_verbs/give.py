#!moo verb give donate offer feed --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-GIVE

parser = context.parser

pre_x = "pre_give"
if _.zork_thing.invoke_verb(pre_x):
    return
if not (parser.get_iobj().flag("actorbit") if parser.has_iobj() else None):
    print(
        "You can't give a "
        + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
        + " to a "
        + (parser.get_iobj().desc() if parser.has_iobj() else "")
        + "!"
    )
    return
else:
    print("The " + (parser.get_iobj().desc() if parser.has_iobj() else "") + " refuses it politely.")
    return
