#!moo verb through --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-THROUGH
# params: OBJ
# aux: M

player = context.player
parser = context.parser
obj = args[0] if len(args) > 0 else None
m = 0

player.zstate_get("#DECL")
if (parser.get_dobj() if parser.has_dobj_str() else None).flag("is_door") and (
    m := _.zork_thing.other_side((parser.get_dobj() if parser.has_dobj_str() else None))
):
    _.walk(m)
    return True
elif not obj and (parser.get_dobj() if parser.has_dobj_str() else None).flag("vehicle"):
    _.perform("board", (parser.get_dobj() if parser.has_dobj_str() else None), None)
    return True
elif obj or not (parser.get_dobj() if parser.has_dobj_str() else None).flag("takeable"):
    # ZIL: <NULL-F ...>
    _.zork_thing.null_f()
    print(
        "You hit your head against the "
        + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
        + " as you attempt this feat."
    )
    return
elif (parser.get_dobj() if parser.has_dobj_str() else None).location == player:
    print("That would involve quite a contortion!")
    return
else:
    print(_.pick(player.zstate_get("YUKS")))
    return
