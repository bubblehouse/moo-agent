#!moo verb alarm wake awake surprise startle --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-ALARM

parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to alarm?")
    return

if prso.flag("actorbit") if prso else None:
    if (prso.getp("strength") if prso else None) < 0:
        print("The " + (prso.desc() if prso else "") + " is rudely awakened.")
        # ZIL: <AWAKEN ...>
        return _.zork_thing.awaken(prso)
    else:
        print("He's wide awake, or haven't you noticed...")
        return
else:
    print("The " + (prso.desc() if prso else "") + " isn't sleeping.")
    return
