#!moo verb open --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-OPEN
# aux: F, STR

parser = context.parser
f = 0
str_v = 0

if (parser.get_dobj() if parser.has_dobj_str() else None).flag("contbit") and not (
    parser.get_dobj() if parser.has_dobj_str() else None
).getp("capacity") == 0:
    if (parser.get_dobj() if parser.has_dobj_str() else None).flag("open"):
        print("It is already open.")
        return
    else:
        (parser.get_dobj() if parser.has_dobj_str() else None).set_flag("open", True)
        (parser.get_dobj() if parser.has_dobj_str() else None).set_flag("touchbit", True)
        if not (parser.get_dobj() if parser.has_dobj_str() else None).contents.first() or (
            parser.get_dobj() if parser.has_dobj_str() else None
        ).flag("transparent"):
            print("Opened.")
            return
        elif (
            (f := (parser.get_dobj() if parser.has_dobj_str() else None).contents.first())
            and not _.next_sibling(f)
            and not f.flag("touchbit")
            and (str_v := f.getp("first_description"))
        ):
            print("The " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " opens.")
            print(str_v)
            return
        else:
            print("Opening the " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " reveals ", end="")
            # ZIL: <PRINT-CONTENTS ...>
            _.zork_thing.print_contents((parser.get_dobj() if parser.has_dobj_str() else None))
            print(".")
            return
elif (parser.get_dobj() if parser.has_dobj_str() else None).flag("is_door"):
    if (parser.get_dobj() if parser.has_dobj_str() else None).flag("open"):
        print("It is already open.")
        return
    else:
        print("The " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " opens.")
        return (parser.get_dobj() if parser.has_dobj_str() else None).set_flag("open", True)
else:
    print("You must tell me how to do that to a " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + ".")
    return
