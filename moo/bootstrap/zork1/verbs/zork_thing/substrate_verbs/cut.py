#!moo verb cut slice pierce --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-CUT

player = context.player
parser = context.parser

if (parser.get_dobj() if parser.has_dobj_str() else None).flag("actorbit"):
    return _.perform(
        "attack",
        (parser.get_dobj() if parser.has_dobj_str() else None),
        (parser.get_iobj() if parser.has_iobj() else None),
    )
elif (parser.get_dobj() if parser.has_dobj_str() else None).flag("flammable") and (
    parser.get_iobj().flag("weapon") if parser.has_iobj() else None
):
    if player.location == (parser.get_dobj() if parser.has_dobj_str() else None):
        print("Not a bright idea, especially since you're in it.")
        return True
    _.remove((parser.get_dobj() if parser.has_dobj_str() else None))
    print(
        "Your skillful "
        + (parser.get_iobj().desc() if parser.has_iobj() else "")
        + "smanship slices the "
        + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
        + " into innumerable slivers which blow away."
    )
    return
elif not (parser.get_iobj().flag("weapon") if parser.has_iobj() else None):
    print('The "cutting edge" of a ' + (parser.get_iobj().desc() if parser.has_iobj() else "") + " is hardly adequate.")
    return
else:
    print("Strange concept, cutting the " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + "....")
    return
