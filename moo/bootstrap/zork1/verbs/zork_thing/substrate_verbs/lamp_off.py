#!moo verb lamp_off extinguish douse --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-LAMP-OFF

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to lamp off?")
    return

if prso.flag("lit") if prso else None:
    if not (prso.flag("onbit") if prso else None):
        print("It is already off.")
    else:
        (prso.set_flag("onbit", False) if prso else None)
        if player.zstate_get("LIT"):
            player.zstate_set("LIT", _.zork_thing.is_lit(player.here()))
        print("The " + (prso.desc() if prso else "") + " is now off.")
        if not player.zstate_get("LIT"):
            print("It is now pitch black.")
else:
    print("You can't turn that off.")
return True
