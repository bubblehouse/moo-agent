#!moo verb overboard --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: V-OVERBOARD
# aux: LOCN

player = context.player
parser = context.parser
locn = 0

if (parser.get_iobj() if parser.has_iobj() else None) == lookup("teeth"):
    if (locn := player.location).flag("vehicle"):
        (parser.get_dobj() if parser.has_dobj_str() else None).moveto(locn.location)
        print("Ahoy -- " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " overboard!")
        return
    else:
        print("You're not in anything!")
        return
elif player.location.flag("vehicle"):
    _.perform("throw", (parser.get_dobj() if parser.has_dobj_str() else None), None)
    return True
else:
    print("Huh?")
    return
