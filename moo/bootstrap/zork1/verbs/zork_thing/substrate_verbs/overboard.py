#!moo verb overboard --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL routine: V-OVERBOARD
# aux: LOCN

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
locn = 0

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to overboard?")
    return

if prsi == lookup("teeth"):
    if (locn := player.location).flag("vehicle"):
        (prso.moveto(locn.location) if prso else None)
        print("Ahoy -- " + (prso.desc() if prso else "") + " overboard!")
        return
    else:
        print("You're not in anything!")
        return
elif player.location.flag("vehicle"):
    _.perform("throw", prso, None)
    return True
else:
    print("Huh?")
    return
