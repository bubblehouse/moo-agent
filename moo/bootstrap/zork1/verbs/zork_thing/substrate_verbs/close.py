#!moo verb close --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-CLOSE

player = context.player
parser = context.parser

if not (parser.get_dobj() if parser.has_dobj_str() else None).flag("contbit") and not (
    parser.get_dobj() if parser.has_dobj_str() else None
).flag("is_door"):
    print("You must tell me how to do that to a " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + ".")
    return
elif (
    not (parser.get_dobj() if parser.has_dobj_str() else None).flag("surfacebit")
    and not (parser.get_dobj() if parser.has_dobj_str() else None).getp("capacity") == 0
):
    if (parser.get_dobj() if parser.has_dobj_str() else None).flag("open"):
        (parser.get_dobj() if parser.has_dobj_str() else None).set_flag("open", False)
        print("Closed.")
        if player.zstate_get("LIT") and not player.zstate_set("LIT", _.zork_thing.is_lit(player.here())):
            print("It is now pitch black.")
        return True
    else:
        print("It is already closed.")
        return
elif (parser.get_dobj() if parser.has_dobj_str() else None).flag("is_door"):
    if (parser.get_dobj() if parser.has_dobj_str() else None).flag("open"):
        (parser.get_dobj() if parser.has_dobj_str() else None).set_flag("open", False)
        print("The " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " is now closed.")
        return
    else:
        print("It is already closed.")
        return
else:
    print("You cannot close that.")
    return
