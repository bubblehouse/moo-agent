#!moo verb close shut --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-CLOSE

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to close?")
    return

if not (prso.flag("contbit") if prso else None) and not (prso.flag("is_door") if prso else None):
    print("You must tell me how to do that to a " + (prso.desc() if prso else "") + ".")
    return
elif not (prso.flag("surfacebit") if prso else None) and not (prso.getp("capacity") if prso else None) == 0:
    if prso.flag("open") if prso else None:
        (prso.set_flag("open", False) if prso else None)
        print("Closed.")
        if player.zstate_get("LIT") and not player.zstate_set("LIT", _.zork_thing.is_lit(player.here())):
            print("It is now pitch black.")
        return True
    else:
        print("It is already closed.")
        return
elif prso.flag("is_door") if prso else None:
    if prso.flag("open") if prso else None:
        (prso.set_flag("open", False) if prso else None)
        print("The " + (prso.desc() if prso else "") + " is now closed.")
        return
    else:
        print("It is already closed.")
        return
else:
    print("You cannot close that.")
    return
