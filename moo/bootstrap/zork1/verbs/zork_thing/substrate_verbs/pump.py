#!moo verb pump --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: V-PUMP

player = context.player
parser = context.parser
pump = lookup("pump")

if (parser.get_iobj() if parser.has_iobj() else None) and not (
    parser.get_iobj() if parser.has_iobj() else None
) == pump:
    print("Pump it up with a " + (parser.get_iobj().desc() if parser.has_iobj() else "") + "?")
    return
elif pump.location == player:
    return _.perform("inflate", (parser.get_dobj() if parser.has_dobj_str() else None), pump)
else:
    print("It's really not clear how.")
    return
