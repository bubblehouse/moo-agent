#!moo verb look_on --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-LOOK-ON

parser = context.parser

if (parser.get_dobj() if parser.has_dobj_str() else None).flag("surfacebit"):
    _.perform("look_inside", (parser.get_dobj() if parser.has_dobj_str() else None), None)
    return True
else:
    print("Look on a " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + "???")
    return
