#!moo verb dig --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL routine: V-DIG

parser = context.parser
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None

if not prsi:
    # SETG of parser-state slot is a no-op in DjangoMOO
    pass
if prsi == lookup("shovel"):
    print("There's no reason to be digging here.")
    return True
if prsi.flag("toolbit") if prsi else None:
    print("Digging with the " + (prsi.desc() if prsi else "") + " is slow and tedious.")
    return
else:
    print("Digging with a " + (prsi.desc() if prsi else "") + " is silly.")
    return
