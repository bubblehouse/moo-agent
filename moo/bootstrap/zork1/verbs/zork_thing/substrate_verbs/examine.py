#!moo verb examine describe what whats --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-EXAMINE

parser = context.parser

if (parser.get_dobj() if parser.has_dobj_str() else None).getp("text"):
    print((parser.get_dobj() if parser.has_dobj_str() else None).getp("text"))
    return
elif (parser.get_dobj() if parser.has_dobj_str() else None).flag("contbit") or (
    parser.get_dobj() if parser.has_dobj_str() else None
).flag("is_door"):
    # ZIL: <V-LOOK-INSIDE ...>
    return _.zork_thing.look_inside()
else:
    print("There's nothing special about the " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + ".")
    return
