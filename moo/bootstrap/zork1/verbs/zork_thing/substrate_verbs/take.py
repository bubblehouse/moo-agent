#!moo verb take get hold carry remove grab catch --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-TAKE

parser = context.parser

pre_x = "pre_take"
if _.zork_thing.invoke_verb(pre_x):
    return
if _.zork_thing.itake() == True:
    if (parser.get_dobj() if parser.has_dobj_str() else None).flag("wearbit"):
        print("You are now wearing the " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + ".")
        return
    else:
        print("Taken.")
        return
