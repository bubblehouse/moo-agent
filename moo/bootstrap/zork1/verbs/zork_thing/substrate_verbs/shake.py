#!moo verb shake --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-SHAKE

player = context.player
parser = context.parser

if (parser.get_dobj() if parser.has_dobj_str() else None).flag("actorbit"):
    print("This seems to have no effect.")
    return
elif not (parser.get_dobj() if parser.has_dobj_str() else None).flag("takeable"):
    print("You can't take it; thus, you can't shake it!")
    return
elif (parser.get_dobj() if parser.has_dobj_str() else None).flag("contbit"):
    if (parser.get_dobj() if parser.has_dobj_str() else None).flag("open"):
        if (parser.get_dobj() if parser.has_dobj_str() else None).contents.first():
            # ZIL: <SHAKE-LOOP ...>
            _.zork_thing.shake_loop()
            print(
                "The contents of the " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " spill ",
                end="",
            )
            if (
                player.here().flag("nonlandbit")
                if player.zstate_get("ZORK-NUMBER") == 3
                else not player.here().flag("outdoor")
            ):
                print("out and disappear", end="")
            else:
                print("to the ground", end="")
            print(".")
            return
        else:
            print("Shaken.")
            return
    else:
        if (parser.get_dobj() if parser.has_dobj_str() else None).contents.first():
            print(
                "It sounds like there is something inside the "
                + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
                + "."
            )
            return
        else:
            print("The " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + " sounds empty.")
            return
else:
    print("Shaken.")
    return
