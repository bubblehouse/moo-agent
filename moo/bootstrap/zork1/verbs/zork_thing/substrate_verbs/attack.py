#!moo verb attack fight hurt injure hit kill murder slay dispatch --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: V-ATTACK

player = context.player
parser = context.parser

if not (parser.get_dobj() if parser.has_dobj_str() else None).flag("actorbit"):
    print(
        "I've known strange people, but fighting a "
        + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
        + "?"
    )
    return
elif not (parser.get_iobj() if parser.has_iobj() else None) or (
    parser.get_iobj() if parser.has_iobj() else None
) == lookup("hands"):
    print(
        "Trying to attack a "
        + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
        + " with your bare hands is suicidal."
    )
    return
elif not (parser.get_iobj() if parser.has_iobj() else None).location == player:
    print("You aren't even holding the " + (parser.get_iobj().desc() if parser.has_iobj() else "") + ".")
    return
elif not (parser.get_iobj().flag("weapon") if parser.has_iobj() else None):
    print(
        "Trying to attack the "
        + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
        + " with a "
        + (parser.get_iobj().desc() if parser.has_iobj() else "")
        + " is suicidal."
    )
    return
else:
    # ZIL: <HERO-BLOW ...>
    return _.zork_thing.hero_blow()
