#!moo verb hello hi --on "Zork Actor" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-HELLO

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None

if prso:
    if prso.flag("actorbit") if prso else None:
        print("The " + (prso.desc() if prso else "") + " bows his head to you in greeting.")
        return
    else:
        print(
            'It\'s a well known fact that only schizophrenics say "Hello" to a ' + (prso.desc() if prso else "") + "."
        )
        return
else:
    print(_.pick(player.zstate_get("HELLOS")))
    return
