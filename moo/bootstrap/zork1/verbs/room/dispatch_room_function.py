#!moo verb dispatch_room_function --on "Room" --dspec none

# Generated by moo/zil_import — Phase 3 scaffolding for the
# syntax-table dispatcher refactor.  Phase 7 grows this into a real
# router.

"""
Route an M-/F-constant action-phase hook through the room's callback.

Called by the syntax-row action-chain runner at M-BEG (before V-*) and
M-END (after V-*).  Returning truthy at M-BEG short-circuits the chain
(matches ZIL room-function returning T to block the default action).

Phase 3 stub: always returns ``False`` so the action chain proceeds
unchanged.  Phase 7 will look up ``loc.action`` (the routine name set
by the ROOM-FUNCTION declaration in ZIL source) and invoke the
matching callback under ``verbs/room/room_functions/<name>.py`` with
the M-/F-constant as ``args[0]``.

:param args: ``(loc, m_constant, verb, dobj, iobj)`` — the action-phase
    snapshot.  Ignored in this Phase 3 stub.
:returns: ``False`` always (pass-through to the next chain step).
"""

return False
