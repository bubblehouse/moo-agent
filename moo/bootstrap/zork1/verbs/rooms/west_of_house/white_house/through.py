#!moo verb through open --on "white house" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name, lookup

# ZIL routine: WHITE-HOUSE-F (through/open branch)

player = context.player
kitchen_window = lookup("kitchen_window")
the_player_verb = invoked_verb_name(verb_name)

if player.here() == lookup("east_of_house"):
    if kitchen_window.flag("open"):
        return _.goto(lookup("kitchen"))
    else:
        print("The window is closed.")
        # ZIL: <THIS-IS-IT ...>
        return _.zork_thing.this_is_it(kitchen_window)
else:
    print("I can't see how to get in from here.")
    return
