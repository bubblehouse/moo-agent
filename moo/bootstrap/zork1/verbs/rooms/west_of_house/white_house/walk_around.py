#!moo verb walk_around where --on "white house" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name, lookup

# ZIL routine: WHITE-HOUSE-F

player = context.player
the_player_verb = invoked_verb_name(verb_name)

if player.here() in (lookup("kitchen"), lookup("living_room"), lookup("attic")):
    if the_player_verb in ["find", "where"]:
        print("Why not find your brains?")
        return
    elif the_player_verb == "walk_around":
        # ZIL: <GO-NEXT ...>
        _.thing.go_next(player.zstate_get("IN-HOUSE-AROUND"))
        return True
elif not (
    player.here() in (lookup("east_of_house"), lookup("west_of_house"))
    or player.here() in (lookup("north_of_house"), lookup("south_of_house"))
):
    if the_player_verb in ["find", "where"]:
        if player.here() == lookup("clearing"):
            print("It seems to be to the west.")
            return
        else:
            print("It was here just a minute ago....")
            return
    else:
        print("You're not at the house.")
        return
return passthrough()
