#!moo verb skeleton --on "skeleton" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL: SKELETON / combined OBJECT-FUNCTION dispatch
# Invoked by ``_.thing.dispatch_object_function(obj, verb, prep, iobj)``
# which looks up ``obj.action`` and calls ``obj.invoke_verb(action_atom, ...)``.

player = context.player
land_of_living_dead = lookup("land_of_living_dead")
the_verb = args[0] if len(args) > 0 else None
prep = args[1] if len(args) > 1 else None
iobj = args[2] if len(args) > 2 else None
the_player_verb = the_verb

if (
    the_verb == "take"
    or the_verb == "rub"
    or the_verb == "move"
    or the_verb == "push"
    or the_verb == "raise"
    or the_verb == "lower"
    or the_verb == "attack"
    or the_verb == "kick"
    or the_verb == "kiss"
):
    print(
        "A ghost appears in the room and is appalled at your desecration of\nthe remains of a fellow adventurer. He casts a curse on your valuables\nand banishes them to the Land of the Living Dead. The ghost leaves,\nmuttering obscenities."
    )
    # ZIL: <ROB ...>
    _.thing.rob(player.here(), land_of_living_dead, 100)
    # ZIL: <ROB ...>
    _.thing.rob(player, land_of_living_dead)
    return True
return False
