#!moo verb attack cut fight hit kill slice stab swing --on "rusty knife" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name

# ZIL routine: RUSTY-KNIFE-FCN

parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
the_player_verb = invoked_verb_name(verb_name)

if (prsi == this and the_player_verb in ["attack", "kill", "hit", "fight", "stab", "cut", "slice"]) or (
    the_player_verb == "swing" and prso == this and prsi
):
    _.remove(this)
    return _.jigs_up(
        "As the knife approaches its victim, your mind is submerged by an\novermastering will. Slowly, your hand turns, until the rusty blade\nis an inch from your neck. The knife seems to sing as it savagely\nslits your throat."
    )
return passthrough()
