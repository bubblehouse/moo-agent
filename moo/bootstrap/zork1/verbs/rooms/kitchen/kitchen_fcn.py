#!moo verb kitchen_fcn look preturnfunc --on "Kitchen" --dspec either

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name, lookup

# ZIL: KITCHEN-FCN / combined M-/F-clause dispatch

player = context.player
parser = context.parser
prso = None
if context.parser is not None:
    try:
        prso = parser.get_dobj() if parser.has_dobj_str() else None
    except NoSuchObjectError:
        prso = None
stairs = lookup("stairs")
verb_to_const = {"look": "M-LOOK", "preturnfunc": "M-BEG"}
rarg = args[0] if len(args) > 0 else verb_to_const.get(verb_name, "M-LOOK")
the_player_verb = args[1] if len(args) > 1 else invoked_verb_name(verb_name)

if rarg == "M-LOOK":
    print(this.desc())
    print(
        "You are in the kitchen of the white house. A table seems to\nhave been used recently for the preparation of food. A passage\nleads to the west and a dark staircase can be seen leading\nupward. A dark chimney leads down and to the east is a small\nwindow which is ",
        end="",
    )
    if lookup("kitchen_window").flag("open"):
        print("open.")
    else:
        print("slightly ajar.")
    _.thing.describe_objects(True)
elif rarg == "M-BEG":
    if the_player_verb == "climb_up" and prso == stairs:
        _.walk(player.zstate_get("P?UP"))
        return True
    elif the_player_verb == "climb_up" and prso == stairs:
        print("There are no stairs leading down.")
        return True
