#!moo verb preturnfunc --on "Kitchen" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL: KITCHEN-FCN / M-BEG

player = context.player
parser = context.parser
stairs = lookup("stairs")
rarg = args[0] if len(args) > 0 else "M-BEG"
the_player_verb = (
    args[1]
    if len(args) > 1
    else (parser.words[0].lower() if context.parser is not None and parser.words else verb_name)
)

if the_player_verb == "climb-up" and (parser.get_dobj() if parser.has_dobj_str() else None) == stairs:
    _.walk(player.zstate_get("P?UP"))
    return True
elif the_player_verb == "climb-up" and (parser.get_dobj() if parser.has_dobj_str() else None) == stairs:
    print("There are no stairs leading down.")
    return True
