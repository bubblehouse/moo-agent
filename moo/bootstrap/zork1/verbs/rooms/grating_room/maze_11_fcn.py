#!moo verb maze_11_fcn enterfunc look --on "Grating Room" --dspec either

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name, lookup

# ZIL: MAZE-11-FCN / combined M-/F-clause dispatch

player = context.player
grate = lookup("grate")
verb_to_const = {"enterfunc": "M-ENTER", "look": "M-LOOK"}
rarg = args[0] if len(args) > 0 else verb_to_const.get(verb_name, "M-ENTER")
the_player_verb = args[1] if len(args) > 1 else invoked_verb_name(verb_name)

if rarg == "M-ENTER":
    grate.set_flag("invisible", False)
elif rarg == "M-LOOK":
    print(this.desc())
    print("You are in a small room near the maze. There are twisty passages\nin the immediate vicinity.")
    if grate.flag("open"):
        print("Above you is an open grating with sunlight pouring in.", end="")
    elif player.zstate_get("GRUNLOCK"):
        print("Above you is a grating.", end="")
    else:
        print("Above you is a grating locked with a skull-and-crossbones lock.", end="")
    print()
    _.thing.describe_objects(True)
