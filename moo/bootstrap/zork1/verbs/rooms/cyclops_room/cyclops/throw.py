#!moo verb throw toss attack kill hit fight stab cut slice mung destroy break smash crack --on "cyclops" --dspec any --ispec on:this in:this at:this with:this from:this under:this over:this before:this around:this through:this behind:this beside:this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name

# ZIL routine: CYCLOPS-FCN (throw/attack/mung branch)

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
count = 0
the_player_verb = invoked_verb_name(verb_name)

if this not in (prso, prsi):
    return passthrough()

_.queue("i-cyclops", -1)
if the_player_verb in ["mung", "destroy", "break", "smash", "crack"]:
    print('"Do you think I\'m as stupid as my father was?", he says, dodging.')
    return
else:
    print("The cyclops shrugs but otherwise ignores your pitiful attempt.")
    if the_player_verb in ["throw", "toss"]:
        prso.moveto(player.here())
    return True
