#!moo verb alarm burn kick light odysseus --on "cyclops" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name

# ZIL routine: CYCLOPS-FCN
# aux: COUNT

player = context.player
count = 0
the_player_verb = invoked_verb_name(verb_name)

count = player.zstate_get("CYCLOWRATH")
if player == this:
    if player.zstate_get("CYCLOPS-FLAG"):
        print("No use talking to him. He's fast asleep.")
        return
    elif the_player_verb == "odysseus":
        player.zstate_set("WINNER", player)
        _.perform("odysseus", None, None)
        return True
    else:
        print("The cyclops prefers eating to making conversation.")
        return
elif player.zstate_get("CYCLOPS-FLAG"):
    if the_player_verb in ["examine", "x", "describe", "what"]:
        print("The cyclops is sleeping like a baby, albeit a very ugly one.")
        return
    elif the_player_verb in [
        "alarm",
        "kick",
        "attack",
        "kill",
        "hit",
        "fight",
        "stab",
        "cut",
        "slice",
        "burn",
        "light",
        "mung",
        "destroy",
        "break",
        "smash",
        "crack",
    ]:
        print("The cyclops yawns and stares at the thing that woke him up.")
        player.zstate_set("CYCLOPS-FLAG", False)
        this.set_flag("hostile", True)
        if count < 0:
            return player.zstate_set("CYCLOWRATH", (-count))
        else:
            return player.zstate_set("CYCLOWRATH", count)
return passthrough()
