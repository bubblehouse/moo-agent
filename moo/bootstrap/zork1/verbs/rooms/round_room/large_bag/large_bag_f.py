#!moo verb large_bag_f --on "large bag" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL: LARGE-BAG-F / combined OBJECT-FUNCTION dispatch
# Invoked by ``_.thing.dispatch_object_function(obj, verb, prep, iobj)``
# which looks up ``obj.action`` and calls ``obj.invoke_verb(action_atom, ...)``.

player = context.player
parser = context.parser
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
the_verb = args[0] if len(args) > 0 else None
prep = args[1] if len(args) > 1 else None
iobj = args[2] if len(args) > 2 else None
the_player_verb = the_verb

if the_verb == "take":
    if lookup("thief").getp("description") == player.zstate_get("ROBBER-U-DESC"):
        print("Sadly for you, the robber collapsed on top of the bag. Trying to take\nit would wake him.")
        return
    else:
        print("The bag will be taken over his dead body.")
        return
elif the_verb == "put":
    if prsi == this:
        print("It would be a good trick.")
        return
elif the_verb == "open" or the_verb == "close":
    print("Getting close enough would be a good trick.")
    return
elif the_verb == "examine" or the_verb == "look_inside":
    print("The bag is underneath the thief, so one can't say what, if anything, is\ninside.")
    return
return False
