#!moo verb look --on "Behind House" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import invoked_verb_name, lookup

# ZIL: EAST-HOUSE / M-LOOK

rarg = args[0] if len(args) > 0 else "M-LOOK"
the_player_verb = args[1] if len(args) > 1 else invoked_verb_name(verb_name)

print(this.desc())
print(
    "You are behind the white house. A path leads into the forest\nto the east. In one corner of the house there is a small window\nwhich is ",
    end="",
)
if lookup("kitchen_window").flag("open"):
    print("open.", end="")
else:
    print("slightly ajar.", end="")
print()
_.thing.describe_objects(True)
