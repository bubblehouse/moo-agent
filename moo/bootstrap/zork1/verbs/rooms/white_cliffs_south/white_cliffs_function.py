#!moo verb white_cliffs_function turnfunc --on "White Cliffs Beach (WHITE-CLIFFS-SOUTH)" --dspec either

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name, lookup

# ZIL: WHITE-CLIFFS-FUNCTION / combined M-/F-clause dispatch

player = context.player
verb_to_const = {"turnfunc": "M-END"}
rarg = args[0] if len(args) > 0 else verb_to_const.get(verb_name, "M-END")
the_player_verb = args[1] if len(args) > 1 else invoked_verb_name(verb_name)

if rarg == "M-END":
    if lookup("inflated_boat").location == player:
        player.zstate_set("DEFLATE", False)
    else:
        player.zstate_set("DEFLATE", True)
