#!moo verb basket_f --on "basket (RAISED-BASKET)" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL: BASKET-F / combined OBJECT-FUNCTION dispatch
# Invoked by ``_.thing.dispatch_object_function(obj, verb, prep, iobj)``
# which looks up ``obj.action`` and calls ``obj.invoke_verb(action_atom, ...)``.

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
lower_shaft = lookup("lower_shaft")
lowered_basket = lookup("lowered_basket")
shaft_room = lookup("shaft_room")
the_verb = args[0] if len(args) > 0 else None
prep = args[1] if len(args) > 1 else None
iobj = args[2] if len(args) > 2 else None
the_player_verb = the_verb

if the_verb == "raise":
    if player.zstate_get("CAGE-TOP"):
        print(_.pick(player.zstate_get("DUMMY")))
        return
    else:
        this.moveto(shaft_room)
        lowered_basket.moveto(lower_shaft)
        player.zstate_set("CAGE-TOP", True)
        # ZIL: <THIS-IS-IT ...>
        _.thing.this_is_it(this)
        print("The basket is raised to the top of the shaft.")
        return
elif the_verb == "lower":
    if not player.zstate_get("CAGE-TOP"):
        print(_.pick(player.zstate_get("DUMMY")))
        return
    else:
        this.moveto(lower_shaft)
        lowered_basket.moveto(shaft_room)
        # ZIL: <THIS-IS-IT ...>
        _.thing.this_is_it(lowered_basket)
        print("The basket is lowered to the bottom of the shaft.")
        player.zstate_set("CAGE-TOP", False)
        if player.zstate_get("LIT") and not player.zstate_set("LIT", _.thing.is_lit(player.here())):
            print("It is now pitch black.")
        return True
elif the_verb == "take":
    if prso in (this, lowered_basket):
        print("The cage is securely fastened to the iron chain.")
        return
return False
