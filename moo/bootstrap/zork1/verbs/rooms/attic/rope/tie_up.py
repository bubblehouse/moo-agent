#!moo verb tie-up --on "rope" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: ROPE-FUNCTION (tie-up branch)

parser = context.parser
rloc = 0
the_player_verb = context.parser.words[0].lower() if context.parser is not None and context.parser.words else verb_name

if this == (parser.get_iobj() if parser.has_iobj() else None):
    if (parser.get_dobj() if parser.has_dobj_str() else None).flag("actorbit"):
        if (parser.get_dobj() if parser.has_dobj_str() else None).getp("strength") < 0:
            print(
                "Your attempt to tie up the "
                + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
                + " awakens him.",
                end="",
            )
            # ZIL: <AWAKEN ...>
            return _.zork_thing.awaken((parser.get_dobj() if parser.has_dobj_str() else None))
        else:
            print(
                "The "
                + (parser.get_dobj() if parser.has_dobj_str() else None).desc()
                + " struggles and you cannot tie him up."
            )
            return
    else:
        print("Why would you tie up a " + (parser.get_dobj() if parser.has_dobj_str() else None).desc() + "?")
        return
return passthrough()
