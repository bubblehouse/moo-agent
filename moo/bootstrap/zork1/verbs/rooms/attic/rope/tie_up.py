#!moo verb tie_up --on "rope" --dspec any --ispec on:this in:this at:this with:this from:this under:this over:this before:this around:this through:this behind:this beside:this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name

# ZIL routine: ROPE-FUNCTION (tie-up branch)

parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
rloc = 0
the_player_verb = invoked_verb_name(verb_name)

if this not in (prso, prsi):
    return passthrough()

if this == prsi:
    if prso.flag("actorbit") if prso else None:
        if (prso.getp("strength") if prso else None) < 0:
            print("Your attempt to tie up the " + (prso.desc() if prso else "") + " awakens him.", end="")
            # ZIL: <AWAKEN ...>
            return _.thing.awaken(prso)
        else:
            print("The " + (prso.desc() if prso else "") + " struggles and you cannot tie him up.")
            return
    else:
        print("Why would you tie up a " + (prso.desc() if prso else "") + "?")
        return
return passthrough()
