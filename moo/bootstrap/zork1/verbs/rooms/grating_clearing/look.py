#!moo verb look --on "Clearing (GRATING-CLEARING)" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL: CLEARING-FCN / M-LOOK

player = context.player
parser = context.parser
rarg = args[0] if len(args) > 0 else "M-LOOK"
the_player_verb = (
    args[1]
    if len(args) > 1
    else (parser.words[0].lower() if context.parser is not None and parser.words else verb_name)
)

print("You are in a clearing, with a forest surrounding you on all sides. A\npath leads south.", end="")
if lookup("grate").flag("open"):
    print()
    print("There is an open grating, descending into darkness.", end="")
elif player.zstate_get("GRATE-REVEALED"):
    print()
    print("There is a grating securely fastened into the ground.", end="")
print()
_.zork_thing.describe_objects(True)
