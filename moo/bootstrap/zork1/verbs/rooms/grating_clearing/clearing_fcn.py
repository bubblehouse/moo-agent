#!moo verb clearing_fcn enterfunc look --on "Clearing (GRATING-CLEARING)" --dspec either

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name, lookup

# ZIL: CLEARING-FCN / combined M-/F-clause dispatch

player = context.player
grate = lookup("grate")
verb_to_const = {"enterfunc": "M-ENTER", "look": "M-LOOK"}
rarg = args[0] if len(args) > 0 else verb_to_const.get(verb_name, "M-ENTER")
the_player_verb = args[1] if len(args) > 1 else invoked_verb_name(verb_name)

if rarg == "M-ENTER":
    if not player.zstate_get("GRATE-REVEALED"):
        grate.set_flag("invisible", True)
elif rarg == "M-LOOK":
    print(this.desc())
    print("You are in a clearing, with a forest surrounding you on all sides. A\npath leads south.", end="")
    if grate.flag("open"):
        print()
        print("There is an open grating, descending into darkness.", end="")
    elif player.zstate_get("GRATE-REVEALED"):
        print()
        print("There is a grating securely fastened into the ground.", end="")
    print()
    _.thing.describe_objects(True)
