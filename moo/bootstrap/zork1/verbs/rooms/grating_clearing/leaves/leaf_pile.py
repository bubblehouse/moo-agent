#!moo verb leaf_pile --on "pile of leaves" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL: LEAF-PILE / combined OBJECT-FUNCTION dispatch
# Invoked by ``_.thing.dispatch_object_function(obj, verb, prep, iobj)``
# which looks up ``obj.action`` and calls ``obj.invoke_verb(action_atom, ...)``.

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
the_verb = args[0] if len(args) > 0 else None
prep = args[1] if len(args) > 1 else None
iobj = args[2] if len(args) > 2 else None
the_player_verb = the_verb

if the_verb == "count":
    print("There are 69,105 leaves here.")
    return
elif the_verb == "burn":
    # ZIL: <LEAVES-APPEAR ...>
    _.thing.leaves_appear()
    _.remove(prso)
    if prso is not None and prso.location == player.here():
        print("The leaves burn.")
        return
    else:
        return _.jigs_up("The leaves burn, and so do you.")
elif the_verb == "cut":
    print("You rustle the leaves around, making quite a mess.")
    # ZIL: <LEAVES-APPEAR ...>
    _.thing.leaves_appear()
    return True
elif the_verb == "move" or the_verb == "take":
    if the_player_verb == "move":
        print("Done.")
    if player.zstate_get("GRATE-REVEALED"):
        return passthrough()
    # ZIL: <LEAVES-APPEAR ...>
    _.thing.leaves_appear()
    if the_player_verb in ["take", "get", "pick"]:
        return passthrough()
    else:
        return True
elif the_verb == "look_under":
    if not player.zstate_get("GRATE-REVEALED"):
        print(
            "Underneath the pile of leaves is a grating. As you release the leaves,\nthe grating is once again concealed from view."
        )
        return
return False
