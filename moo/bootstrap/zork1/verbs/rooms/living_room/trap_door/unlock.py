#!moo verb unlock --on "trap door" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name, lookup

# ZIL routine: TRAP-DOOR-FCN

player = context.player
the_player_verb = invoked_verb_name(verb_name)

if player.here() == lookup("cellar"):
    if the_player_verb in ["open", "unlock"] and not this.flag("open"):
        print("The door is locked from above.")
        return
    elif the_player_verb in ["close", "shut"] and not this.flag("open"):
        this.set_flag("touchbit", False)
        this.set_flag("open", False)
        print("The door closes and locks.")
        return
    elif the_player_verb in ["open", "close", "shut"]:
        print(_.pick(player.zstate_get("DUMMY")))
        return
return passthrough()
