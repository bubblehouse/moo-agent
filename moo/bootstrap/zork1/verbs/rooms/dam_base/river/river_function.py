#!moo verb river_function --on "river" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL: RIVER-FUNCTION / combined OBJECT-FUNCTION dispatch
# Invoked by ``_.thing.dispatch_object_function(obj, verb, prep, iobj)``
# which looks up ``obj.action`` and calls ``obj.invoke_verb(action_atom, ...)``.

parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
the_verb = args[0] if len(args) > 0 else None
prep = args[1] if len(args) > 1 else None
iobj = args[2] if len(args) > 2 else None
the_player_verb = the_verb

if the_verb == "put":
    if prsi == this:
        if prso == lookup("me"):
            return _.jigs_up("You splash around for a while, fighting the current, then you drown.")
        elif prso == lookup("inflated_boat"):
            print("You should get in the boat then launch it.")
            return
        elif prso.flag("flammable") if prso else None:
            _.remove(prso)
            print("The " + (prso.desc() if prso else "") + " floats for a moment, then sinks.")
            return
        else:
            _.remove(prso)
            print("The " + (prso.desc() if prso else "") + " splashes into the water and is gone forever.")
            return
elif the_verb == "leap" or the_verb == "through":
    print(
        "A look before leaping reveals that the river is wide and dangerous,\nwith swift currents and large, half-hidden rocks. You decide to forgo your\nswim."
    )
    return
return False
