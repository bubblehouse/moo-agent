#!moo verb put place insert --on "river" --dspec any --ispec on:this in:this at:this with:this from:this under:this over:this before:this around:this through:this behind:this beside:this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name, lookup

# ZIL routine: RIVER-FUNCTION (put branch)

parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
the_player_verb = invoked_verb_name(verb_name)

if this not in (prso, prsi):
    return passthrough()

if prsi == this:
    if prso == lookup("me"):
        return _.jigs_up("You splash around for a while, fighting the current, then you drown.")
    elif prso == lookup("inflated_boat"):
        print("You should get in the boat then launch it.")
        return
    elif prso.flag("flammable") if prso else None:
        _.remove(prso)
        print("The " + (prso.desc() if prso else "") + " floats for a moment, then sinks.")
        return
    else:
        _.remove(prso)
        print("The " + (prso.desc() if prso else "") + " splashes into the water and is gone forever.")
        return
return passthrough()
