#!moo verb bats_room --on "Zork Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: BATS-ROOM
# params: RARG

player = context.player
rarg = args[0] if len(args) > 0 else None

if rarg == "M-ENTER" and not player.zstate_get("DEAD"):
    if not lookup("garlic").location in (player, player.here()):
        # ZIL: <V-LOOK ...>
        _.zork_thing.look()
        print()
        # ZIL: <FLY-ME ...>
        return _.zork_thing.fly_me()
return passthrough()
