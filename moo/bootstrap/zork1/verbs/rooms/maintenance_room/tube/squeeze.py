#!moo verb squeeze --on "tube" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: TUBE-FUNCTION (squeeze branch)

player = context.player
parser = context.parser
putty = lookup("putty")
the_player_verb = context.parser.words[0].lower() if context.parser is not None and context.parser.words else verb_name

if (parser.get_dobj() if parser.has_dobj_str() else None).flag("open") and putty.location == (
    parser.get_dobj() if parser.has_dobj_str() else None
):
    putty.moveto(player)
    print("The viscous material oozes into your hand.")
    return
elif (parser.get_dobj() if parser.has_dobj_str() else None).flag("open"):
    print("The tube is apparently empty.")
    return
else:
    print("The tube is closed.")
    return
