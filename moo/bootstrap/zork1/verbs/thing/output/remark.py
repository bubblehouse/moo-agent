#!moo verb remark --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, task_time_low

# ZIL routine: REMARK
# params: REMARK, D, W
# aux: LEN, CNT, STR

player = context.player
remark = args[0] if len(args) > 0 else None
d = args[1] if len(args) > 1 else None
w = args[2] if len(args) > 2 else None
len_v = _.table_get(remark, 0)
cnt = 0
str_v = 0

while True:
    if task_time_low():
        print("[zil] long-running loop in REMARK; aborting (bug — please report).")
        return False
    if (cnt := (cnt + 1)) > len_v:
        break
    str_v = _.table_get(remark, cnt)
    if str_v == player.zstate_get("F-WEP"):
        print(w.desc(), end="")
    elif str_v == player.zstate_get("F-DEF"):
        print(d.desc(), end="")
    else:
        print(str_v, end="")
print()
return
