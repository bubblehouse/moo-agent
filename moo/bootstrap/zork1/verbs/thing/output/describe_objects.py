#!moo verb describe_objects --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: DESCRIBE-OBJECTS
# params: V?

player = context.player
v_p = args[0] if len(args) > 0 else None

if player.zstate_get("LIT"):
    if player.here().contents.first():
        # ZIL: <PRINT-CONT ...>
        return _.thing.print_cont(player.here(), (v_p := (v_p or player.zstate_get("VERBOSE"))), -1)
else:
    print("Only bats can see in the dark. And you're not one.")
    return
