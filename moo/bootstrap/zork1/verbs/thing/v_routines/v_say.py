#!moo verb v_say --on "Thing" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: V-SAY
# aux: V

player = context.player
parser = context.parser
v = 0

if not len(parser.words) if context.parser is not None else 0:
    print("Say what?")
    return True
player.zstate_set("QUOTE-FLAG", False)
if v := _.thing.find_in(player.here(), "actorbit"):
    print("You must address the " + v.desc() + " directly.")
    player.zstate_set("P-CONT", False)
elif not (
    parser.words[(len(parser.words) if context.parser is not None else 0) - 1]
    if context.parser is not None and len(parser.words) >= (len(parser.words) if context.parser is not None else 0)
    else ""
) == player.zstate_get("W?HELLO"):
    player.zstate_set("P-CONT", False)
    print("Talking to yourself is a sign of impending mental collapse.")
return True
