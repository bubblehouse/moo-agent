#!moo verb v_lamp_on --on "Thing" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-LAMP-ON

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to lamp on?")
    return

if prso.flag("lit") if prso else None:
    if prso.flag("onbit") if prso else None:
        print("It is already on.")
    else:
        (prso.set_flag("onbit", True) if prso else None)
        print("The " + (prso.desc() if prso else "") + " is now on.")
        if not player.zstate_get("LIT"):
            player.zstate_set("LIT", _.thing.is_lit(player.here()))
            print()
            # ZIL: <V-LOOK ...>
            _.thing.look()
elif prso.flag("flammable") if prso else None:
    print("If you wish to burn the " + (prso.desc() if prso else "") + ", you should say so.")
else:
    print("You can't turn that on.")
return True
