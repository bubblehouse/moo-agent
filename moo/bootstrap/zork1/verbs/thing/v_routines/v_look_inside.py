#!moo verb v_look_inside --on "Thing" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-LOOK-INSIDE

parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to look inside?")
    return

if prso.flag("is_door") if prso else None:
    if prso.flag("open") if prso else None:
        print("The " + (prso.desc() if prso else "") + " is open, but I can't tell what's beyond it.", end="")
    else:
        print("The " + (prso.desc() if prso else "") + " is closed.", end="")
    print()
    return
elif prso.flag("contbit") if prso else None:
    if prso.flag("actorbit") if prso else None:
        print("There is nothing special to be seen.")
        return
    elif _.thing.is_see_inside(prso):
        if prso.contents.first() and _.thing.print_cont(prso):
            return True
        elif _.thing.null_f():
            return True
        else:
            print("The " + (prso.desc() if prso else "") + " is empty.")
            return
    else:
        print("The " + (prso.desc() if prso else "") + " is closed.")
        return
else:
    print("You can't look inside a " + (prso.desc() if prso else "") + ".")
    return
