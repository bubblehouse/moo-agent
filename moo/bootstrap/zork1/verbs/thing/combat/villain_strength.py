#!moo verb villain_strength --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL routine: VILLAIN-STRENGTH
# params: OO
# aux: VILLAIN, OD, TMP

player = context.player
parser = context.parser
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
oo = args[0] if len(args) > 0 else None
villain = _.table_get(oo, player.zstate_get("V-VILLAIN"))
od = 0
tmp = 0

od = villain.getp("strength")
if not od < 0:
    if villain == lookup("thief") and player.zstate_get("THIEF-ENGROSSED"):
        if od > 2:
            od = 2
        player.zstate_set("THIEF-ENGROSSED", False)
    if prsi and (prsi.flag("weapon") if prsi else None) and _.table_get(oo, player.zstate_get("V-BEST")) == prsi:
        tmp = od - _.table_get(oo, player.zstate_get("V-BEST-ADV"))
        if tmp < 1:
            tmp = 1
        od = tmp
return od
