#!moo verb rob --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

import random
from moo.sdk import lookup, task_time_low

# ZIL routine: ROB
# params: WHAT, WHERE, PROB
# aux: N, X, ROBBED?

what = args[0] if len(args) > 0 else None
where = args[1] if len(args) > 1 else None
prob = args[2] if len(args) > 2 else None
n = 0
x = 0
robbed_p = 0

x = what.contents.first()
while True:
    if task_time_low():
        print("[zil] long-running loop in ROB; aborting (bug — please report).")
        return False
    if not x:
        return robbed_p
    n = _.next_sibling(x)
    if (
        not x.flag("invisible")
        and not x.flag("sacred")
        and x.getp("tvalue") > 0
        and (not prob or random.randint(1, 100) <= prob)
    ):
        x.moveto(where)
        x.set_flag("touchbit", True)
        if where == lookup("thief"):
            x.set_flag("invisible", True)
        robbed_p = True
    x = n
