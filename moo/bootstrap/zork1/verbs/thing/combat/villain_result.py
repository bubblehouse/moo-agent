#!moo verb villain_result --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context

# ZIL routine: VILLAIN-RESULT
# params: VILLAIN, DEF, RES

player = context.player
villain = args[0] if len(args) > 0 else None
def_v = args[1] if len(args) > 1 else None
res = args[2] if len(args) > 2 else None

villain.set_property("strength", def_v)
if def_v == 0:
    villain.set_flag("hostile", False)
    print(
        "Almost as soon as the "
        + villain.desc()
        + " breathes his last breath, a cloud\nof sinister black fog envelops him, and when the fog lifts, the\ncarcass has disappeared."
    )
    _.remove(villain)
    # ZIL: <APPLY ...>
    if villain is not None and villain.has_verb("f_dead", recurse=False):
        villain.invoke_verb("f_dead")
    return res
elif res == player.zstate_get("UNCONSCIOUS"):
    # ZIL: <APPLY ...>
    if villain is not None and villain.has_verb("f_unconscious", recurse=False):
        villain.invoke_verb("f_unconscious")
    return res
else:
    return res
