#!moo verb otval_frob --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import lookup, task_time_low

# ZIL routine: OTVAL-FROB
# params: O
# aux: F, SCORE

o = args[0] if len(args) > 0 else lookup("trophy_case")
f = 0
score = 0

f = o.contents.first()
while True:
    if task_time_low():
        print("[zil] long-running loop in OTVAL-FROB; aborting (bug — please report).")
        return False
    if not f:
        return score
    score = score + f.getp("tvalue")
    if f.contents.first():
        # ZIL: <OTVAL-FROB ...>
        _.thing.otval_frob(f)
    f = _.next_sibling(f)
