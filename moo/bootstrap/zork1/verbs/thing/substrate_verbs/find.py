#!moo verb find where seek see --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, lookup

# ZIL routine: V-FIND
# aux: L

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
l = prso.location if prso is not None else None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to find?")
    return

if prso in (lookup("hands"), lookup("lungs")):
    print("Within six feet of your head, assuming you haven't left that\nsomewhere.")
    return
elif prso == lookup("me"):
    print("You're around here somewhere...")
    return
elif l == lookup("global_objects"):
    print("You find it.")
    return
elif prso is not None and prso.location == player:
    print("You have it.")
    return
elif (
    (prso is not None and prso.location == player.here())
    or prso.global_in(player.here())
    or prso == lookup("pseudo_object")
):
    print("It's right here.")
    return
elif l.flag("actorbit"):
    print("The " + l.desc() + " has it.")
    return
elif l.flag("surfacebit"):
    print("It's on the " + l.desc() + ".")
    return
elif l.flag("contbit"):
    print("It's in the " + l.desc() + ".")
    return
else:
    print("Beats me.")
    return
