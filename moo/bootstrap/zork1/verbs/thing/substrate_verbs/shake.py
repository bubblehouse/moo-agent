#!moo verb shake --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-SHAKE

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to shake?")
    return

if prso.flag("actorbit") if prso else None:
    print("This seems to have no effect.")
    return
elif not (prso.flag("takeable") if prso else None):
    print("You can't take it; thus, you can't shake it!")
    return
elif prso.flag("contbit") if prso else None:
    if prso.flag("open") if prso else None:
        if prso.contents.first():
            # ZIL: <SHAKE-LOOP ...>
            _.thing.shake_loop()
            print("The contents of the " + (prso.desc() if prso else "") + " spill ", end="")
            if (
                player.here().flag("nonlandbit")
                if player.zstate_get("ZORK-NUMBER") == 3
                else not player.here().flag("outdoor")
            ):
                print("out and disappear", end="")
            else:
                print("to the ground", end="")
            print(".")
            return
        else:
            print("Shaken.")
            return
    else:
        if prso.contents.first():
            print("It sounds like there is something inside the " + (prso.desc() if prso else "") + ".")
            return
        else:
            print("The " + (prso.desc() if prso else "") + " sounds empty.")
            return
else:
    print("Shaken.")
    return
