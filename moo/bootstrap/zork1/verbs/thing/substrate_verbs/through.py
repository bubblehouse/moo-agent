#!moo verb through --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context

# ZIL routine: V-THROUGH
# params: OBJ
# aux: M

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
obj = args[0] if len(args) > 0 else None
m = 0

if prso is None:
    if context.parser is not None and context.parser.has_dobj_str():
        print("There is no '" + context.parser.dobj_str + "' here.")
    else:
        print("What do you want to through?")
    return

player.zstate_get("#DECL")
if (prso.flag("is_door") if prso else None) and (m := _.thing.other_side(prso)):
    _.walk(m)
    return True
elif not obj and (prso.flag("vehicle") if prso else None):
    _.perform("board", prso, None)
    return True
elif obj or not (prso.flag("takeable") if prso else None):
    # ZIL: <NULL-F ...>
    _.thing.null_f()
    print("You hit your head against the " + (prso.desc() if prso else "") + " as you attempt this feat.")
    return
elif prso is not None and prso.location == player:
    print("That would involve quite a contortion!")
    return
else:
    print(_.pick(player.zstate_get("YUKS")))
    return
