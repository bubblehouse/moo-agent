#!moo verb i_rempty --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: I-REMPTY

player = context.player
deep_canyon = lookup("deep_canyon")
reservoir = lookup("reservoir")

reservoir.set_flag("outdoor", True)
reservoir.set_flag("nonlandbit", False)
deep_canyon.set_flag("touchbit", False)
lookup("loud_room").set_flag("touchbit", False)
lookup("trunk").set_flag("invisible", False)
player.zstate_set("LOW-TIDE", True)
if player.here() == reservoir and player.location.flag("vehicle"):
    print(
        "The water level has dropped to the point at which the boat can no\nlonger stay afloat. It sinks into the mud."
    )
elif player.here() == deep_canyon:
    print("The roar of rushing water is quieter now.")
elif player.here() in (lookup("reservoir_north"), lookup("reservoir_south")):
    print("The water level is now quite low here and you could easily cross over\nto the other side.")
return True
