#!moo verb i_candles --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: I-CANDLES
# aux: TICK, TBL

player = context.player
candles = lookup("candles")
tick = 0
tbl = value(player.zstate_get("CANDLE-TABLE"))

candles.set_flag("touchbit", True)
_.queue("i-candles", (tick := _.table_get(tbl, 0)))
# ZIL: <LIGHT-INT ...>
_.thing.light_int(candles, tbl, tick)
if tick != 0:
    return player.zstate_set("CANDLE-TABLE", _.rest(tbl, 4))
