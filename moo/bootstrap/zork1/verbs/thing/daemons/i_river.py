#!moo verb i_river --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: I-RIVER
# aux: RM

player = context.player
rm = 0

if not player.here() in (lookup("river_1"), lookup("river_2"), lookup("river_3")) and not player.here() in (
    lookup("river_4"),
    lookup("river_5"),
):
    return _.cancel("i-river")
elif rm := _.thing.lkp(player.here(), player.zstate_get("RIVER-NEXT")):
    print("The flow of the river carries you downstream.")
    _.goto(rm)
    return _.queue("i-river", _.thing.lkp(player.here(), player.zstate_get("RIVER-SPEEDS")))
else:
    return _.jigs_up(
        "Unfortunately, the magic boat doesn't provide protection from\nthe rocks and boulders one meets at the bottom of waterfalls.\nIncluding this one."
    )
