#!moo verb up_chimney_function --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: UP-CHIMNEY-FUNCTION
# aux: F

player = context.player
trap_door = lookup("trap_door")
f = 0

if not (f := player.contents.first()):
    print("Going up empty-handed is a bad idea.")
    return False
elif (not (f := _.next_sibling(f)) or not _.next_sibling(f)) and lookup("lamp").location == player:
    if not trap_door.flag("open"):
        trap_door.set_flag("touchbit", False)
    return lookup("kitchen")
else:
    print("You can't get up there with what you're carrying.")
    return False
