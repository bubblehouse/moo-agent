#!moo verb meta_loc --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup, task_time_low

# ZIL routine: META-LOC
# params: OBJ

player = context.player
global_objects = lookup("global_objects")
obj = args[0] if len(args) > 0 else None

while True:
    if task_time_low():
        print("[zil] long-running loop in META-LOC; aborting (bug — please report).")
        return False
    if not obj:
        return False
    elif obj.location == global_objects:
        return global_objects
    if obj.location == player.zstate_get("ROOMS"):
        return obj
    else:
        obj = obj.location
