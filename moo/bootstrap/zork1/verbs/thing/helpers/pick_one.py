#!moo verb pick_one --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

import random

# ZIL routine: PICK-ONE
# params: FROB
# aux: L, CNT, RND, MSG, RFROB

frob = args[0] if len(args) > 0 else None
l = _.table_get(frob, 0)
cnt = _.table_get(frob, 1)
rnd = 0
msg = 0
rfrob = 0

l = l - 1
frob = _.rest(frob, 2)
rfrob = _.rest(frob, (cnt * 2))
rnd = random.randint(1, (l - cnt))
msg = _.table_get(rfrob, rnd)
# ZIL: <PUT ...>
_.table_put(rfrob, rnd, _.table_get(rfrob, 1))
# ZIL: <PUT ...>
_.table_put(rfrob, 1, msg)
cnt = cnt + 1
if cnt == l:
    cnt = 0
# ZIL: <PUT ...>
_.table_put(frob, 0, cnt)
return msg
