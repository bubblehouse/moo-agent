#!moo verb open_close --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, invoked_verb_name

# ZIL routine: OPEN-CLOSE
# params: OBJ, STROPN, STRCLS

player = context.player
obj = args[0] if len(args) > 0 else None
stropn = args[1] if len(args) > 1 else None
strcls = args[2] if len(args) > 2 else None
the_player_verb = invoked_verb_name(verb_name)

if the_player_verb == "open":
    if obj.flag("open"):
        print(_.pick(player.zstate_get("DUMMY")), end="")
    else:
        print(stropn, end="")
        obj.set_flag("open", True)
    print()
    return
elif the_player_verb in ["close", "shut"]:
    if obj.flag("open"):
        print(strcls, end="")
        obj.set_flag("open", False)
        True
    else:
        print(_.pick(player.zstate_get("DUMMY")), end="")
    print()
    return
