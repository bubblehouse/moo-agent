#!moo verb touch_all --on "Thing" --dspec this

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import task_time_low

# ZIL routine: TOUCH-ALL
# params: OBJ
# aux: F

obj = args[0] if len(args) > 0 else None
f = 0

f = obj.contents.first()
while True:
    if task_time_low():
        print("[zil] long-running loop in TOUCH-ALL; aborting (bug — please report).")
        return False
    if not f:
        break
    else:
        f.set_flag("touchbit", True)
        if f.contents.first():
            # ZIL: <TOUCH-ALL ...>
            _.thing.touch_all(f)
    f = _.next_sibling(f)
