#!moo verb find_in --on "Thing" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, task_time_low

# ZIL routine: FIND-IN
# params: WHERE, WHAT
# aux: W

player = context.player
where = args[0] if len(args) > 0 else None
what = args[1] if len(args) > 1 else None
w = 0

w = where.contents.first()
if not w:
    return False
while True:
    if task_time_low():
        print("[zil] long-running loop in FIND-IN; aborting (bug — please report).")
        return False
    if w.flag(what) and w != player:
        return w
    elif not (w := _.next_sibling(w)):
        return False
