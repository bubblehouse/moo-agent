#!moo verb rub --on "red hot brass bell" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name, lookup

# ZIL routine: HOT-BELL-F

parser = context.parser
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
the_player_verb = invoked_verb_name(verb_name)

if the_player_verb == "rub" or (the_player_verb == "ring" and prsi):
    if prsi.flag("flammable") if prsi else None:
        print("The " + (prsi.desc() if prsi else "") + " burns and is consumed.")
        return _.remove(prsi)
    elif prsi == lookup("hands"):
        print("The bell is too hot to touch.")
        return
    else:
        print("The heat from the bell is too intense.")
        return
return passthrough()
