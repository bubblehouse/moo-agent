#!moo verb rboat_function --on "magic boat" --dspec none

# Generated by moo/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL: RBOAT-FUNCTION / combined OBJECT-FUNCTION dispatch
# Invoked by ``_.thing.dispatch_object_function(obj, verb, prep, iobj)``
# which looks up ``obj.action`` and calls ``obj.invoke_verb(action_atom, ...)``.

player = context.player
inflatable_boat = lookup("inflatable_boat")
punctured_boat = lookup("punctured_boat")
the_verb = args[0] if len(args) > 0 else None
prep = args[1] if len(args) > 1 else None
iobj = args[2] if len(args) > 2 else None
rarg = args[0] if len(args) > 0 else None
tmp = 0
the_player_verb = the_verb

if the_verb == "board":
    if (
        lookup("sceptre").location == player
        or lookup("knife").location == player
        or lookup("sword").location == player
        or lookup("rusty_knife").location == player
        or lookup("axe").location == player
        or lookup("stiletto").location == player
    ):
        print(
            "Oops! Something sharp seems to have slipped and punctured the boat.\nThe boat deflates to the sounds of hissing, sputtering, and cursing."
        )
        _.remove(this)
        punctured_boat.moveto(player.here())
        # ZIL: <THIS-IS-IT ...>
        _.thing.this_is_it(punctured_boat)
        return True
elif the_verb == "inflate" or the_verb == "fill":
    print("Inflating it further would probably burst it.")
    return
elif the_verb == "deflate":
    if player.location == this:
        print("You can't deflate the boat while you're in it.")
        return
    elif not this.location == player.here():
        print("The boat must be on the ground to be deflated.")
        return
    else:
        print("The boat deflates.")
        player.zstate_set("DEFLATE", True)
        _.remove(this)
        inflatable_boat.moveto(player.here())
        # ZIL: <THIS-IS-IT ...>
        return _.thing.this_is_it(inflatable_boat)
return False
