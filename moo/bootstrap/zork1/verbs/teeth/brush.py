#!moo verb brush clean --on "set of teeth" --dspec either

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import context, lookup

# ZIL routine: TEETH-F (brush branch)

player = context.player
parser = context.parser
the_player_verb = context.parser.words[0].lower() if context.parser is not None and context.parser.words else verb_name

if (parser.get_dobj() if parser.has_dobj_str() else None) == this:
    if (parser.get_iobj() if parser.has_iobj() else None) == lookup("putty") and (
        parser.get_iobj() if parser.has_iobj() else None
    ).location == player:
        return _.jigs_up(
            "Well, you seem to have been brushing your teeth with some sort of\nglue. As a result, your mouth gets glued together (with your nose)\nand you die of respiratory failure."
        )
    elif not (parser.get_iobj() if parser.has_iobj() else None):
        print("Dental hygiene is highly recommended, but I'm not sure what you want\nto brush them with.")
        return
    else:
        print("A nice idea, but with a " + (parser.get_iobj().desc() if parser.has_iobj() else "") + "?")
        return
return passthrough()
