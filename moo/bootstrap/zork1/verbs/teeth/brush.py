#!moo verb brush clean --on "set of teeth" --dspec this

# Generated by extras/zil_import — do not edit by hand
# pylint: disable=return-outside-function,undefined-variable,no-name-in-module

from moo.sdk import NoSuchObjectError, context, invoked_verb_name, lookup

# ZIL routine: TEETH-F (brush branch)

player = context.player
parser = context.parser
try:
    prso = parser.get_dobj() if parser.has_dobj_str() else None
except NoSuchObjectError:
    prso = None
try:
    prsi = parser.get_iobj() if parser.has_iobj() else None
except NoSuchObjectError:
    prsi = None
the_player_verb = invoked_verb_name(verb_name)

if prso == this:
    if prsi == lookup("putty") and (prsi is not None and prsi.location == player):
        return _.jigs_up(
            "Well, you seem to have been brushing your teeth with some sort of\nglue. As a result, your mouth gets glued together (with your nose)\nand you die of respiratory failure."
        )
    elif not prsi:
        print("Dental hygiene is highly recommended, but I'm not sure what you want\nto brush them with.")
        return
    else:
        print("A nice idea, but with a " + (prsi.desc() if prsi else "") + "?")
        return
return passthrough()
